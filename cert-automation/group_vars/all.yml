---
# Environment-specific variables for ACME certificate acquisition
# These variables apply to all hosts in the inventory

# Domain to obtain certificate for
domain_name: demo.sdunne.net

# Additional domains (SANs) - optional
# additional_domains:
#   - www.demo.sdunne.net
#   - api.demo.sdunne.net

# Email for Let's Encrypt notifications
acme_email: sdunne@redhat.com

# Directory to store certificates
cert_dir: "./certs"

# ACME directory (staging for testing)
acme_directory: https://acme-staging-v02.api.letsencrypt.org/directory

# For production (uncomment when ready):
# acme_directory: https://acme-v02.api.letsencrypt.org/directory

# GoDaddy API credentials (loaded from secrets.yml)
godaddy_api_key: "{{ godaddy_key }}"
godaddy_api_secret: "{{ godaddy_secret }}"
